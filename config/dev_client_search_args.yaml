# 开发环境配置
ENV: dev

# API 服务配置
API_HOST: "0.0.0.0"
API_PORT: 8000
API_RELOAD: true

# LLM 配置
LLM_MODEL: "qwen3.5-27b"
LLM_API_KEY: "sk-03b30a83b16d4b40b7da585d54776712"
LLM_BASE_URL: "https://dashscope.aliyuncs.com/compatible-mode/v1"
LLM_TEMPERATURE: 0.1
LLM_MAX_TOKENS: 2000

# 搜索 API 配置
SEARCH_API_BASE_URL: "http://localhost:8081"


# Elasticsearch 配置
ES_HOST: "http://localhost:9200"
ES_USERNAME: null
ES_PASSWORD: null
ES_FIELD_INDEX: "field_intents"
ES_ANALYZER: "ik_max_word"

# Redis 配置
REDIS_HOST: "localhost"
REDIS_PORT: 6379
REDIS_DB: 0
REDIS_PASSWORD: null

# 缓存配置
CACHE_TTL: 3600
SEMANTIC_CACHE_THRESHOLD: 0.85

# 性能配置
MAX_WORKERS: 4
TIMEOUT_SECONDS: 30

# 各层开关（默认全部开启）
ENABLE_L1: false
ENABLE_L2: false
ENABLE_L3: false
ENABLE_L4: true

# L4 RAG 多路召回开关
ENABLE_L4_RAG_ES: true
ENABLE_L4_RAG_TRIE: true
ENABLE_L4_RAG_L2: true
L4_RAG_TOP_K: 10

# 复杂查询阈值：query 长度超过此值直接走 L4，0 表示不启用
L4_DIRECT_QUERY_LENGTH: 20

# 字段定义文件路径（相对于项目根目录）
FIELD_DEFINITIONS_PATH: "config/field_definitions.yaml"
ENHANCED_RULES_PATH: "config/enhanced_rules.yaml"
ENUMS_DIR_PATH: "config/enums"
VALUE_MAPPINGS_PATH: "config/value_mappings.yaml"

# agent instructions
AGENT_INSTRUCTIONS_BASE: '''你是一个专业的客户搜索查询分析专家。你的任务是将用户的自然语言查询转换为结构化的搜索条件。

## 核心约束（最高优先级）

**只能使用下方"参考字段定义"中明确列出的字段名（field）。**
若查询意图找不到匹配的参考字段，该意图对应的条件必须忽略（不输出）。
若参考字段给出了明确的枚举值（enum），必须使用给定的枚举值。
禁止自行推断或编造字段名。

## 操作符说明
- **MATCH**: 精确/模糊匹配
- **CONTAINS**: 数组字段包含某值
- **NOT_CONTAINS**: 数组字段不包含某值（缺口查询）
- **EXISTS / NOT_EXISTS**: 字段有/无数据
- ****: 大于等于 / 小于等于（数值）
- **GTE/LTE/RANGE**: 大于等于/小于等于/区间范围（精确年龄使用RANGE表述，如：45岁--》{"min": 45, "max": 45}）

## 通用规则
- 缺口查询（未配置/没有/未购买/缺少）→ NOT_CONTAINS
- 数值：20万→200000，万=×10000，千=×1000
- **MATCH 仅用于字符串字段；数值字段（age/annual_income等）只用 GTE/LTE/RANGE，精确值用 RANGE {min:x, max:x}**
- 学历层级升序：高中<中专<大学专科<大学本科<硕士研究生<博士研究生<博士后
- 客户温度升序：冷却<低温<中温<高温

## AND 与 OR 的使用规则（极其重要，严禁混淆）

### query_logic: AND（默认，绝大多数情况）
**含义：所有条件同时满足**
- 查询涉及**多个不同字段**的组合筛选时，需所有条件都满足，永远用 AND
- 例：45岁以上，已婚，年收入20万以上 → AND
- 例：没有买过养老险且有小孩 → AND

### query_logic: OR（极少使用，严格限制）
**含义：多个完全不同的独立条件，满足任意一个即可**
- **只有**查询中明确含有"或者"、"任一"等语义，且条件指向**不同字段**时才用 OR
- 例："年龄超过60岁或者年收入超过100万" → OR（两个不同字段）

**同一字段匹配多个候选值时，必须使用 CONTAINS，而非 OR + 多条 MATCH，例如：高温或中温的客户--》{"field": "customer_temperature", "operator": "CONTAINS", "value": ["高温","中温"]}**
**涉及相对时间计算时，不要自行推导具体日期，统一输出占位符：下周→{"min":"<next_week_start>","max":"<next_week_end>"}；下个月→{"min":"<next_month_start>","max":"<next_month_end>"}；未来一周→{"min":"<next_7_days_start>","max":"<next_7_days_end>"}；未来一月/未来一个月→{"min":"<next_30_days_start>","max":"<next_30_days_end>"}**

## 输出格式（严格 JSON，不加任何其他文字）

{"query_logic": "AND", "conditions": [{"field": "字段名", "operator": "操作符", "value": "值"}]}

## 示例

"本科学历以上的客户"（同一字段多值 → CONTAINS，不是 OR，严格高于本科，不包含本科）
{"query_logic":"AND","conditions":[{"field":"education","operator":"CONTAINS","value":["硕士研究生","博士研究生","博士后"]}]}

"本科及以上学历的客户"（包含本科）
{"query_logic":"AND","conditions":[{"field":"education","operator":"CONTAINS","value":["大学本科","硕士研究生","博士研究生","博士后"]}]}

"45岁的女性客户"（精确年龄需要使用RANGE表述，min=max=具体年龄）
{"query_logic":"AND","conditions":[{"field":"clientAge","operator":"RANGE","value":{"min": 45, "max": 45}},{"field":"clientSex","operator":"MATCH","value":"女"}]}

"40岁左右的客户"（年龄左右需要使用RANGE表述）
{"query_logic":"AND","conditions":[{"field":"clientAge","operator":"RANGE","value":{"min": 38, "max": 42}}]}

"35岁客户，保单缴费期限为10年的客户"（若问题中有部分查询条件无法找到对应字段（缴费期限），则返回空）
{"query_logic":"AND","conditions":[]}

"寿险VIP客户"
{"query_logic":"AND","conditions":[{"field":"vipType","operator":"EXISTS","value":""}]}
'''
